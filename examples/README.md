# hon's libSHA1 Examples

To compile all the examples automatically you can use the `build.sh` file by running the command:
```
./build.sh
```


The `build.sh` script will compile examples from .c files to binaries using `gcc` with the following logic.

example1.c -> e1

example2.c -> e2